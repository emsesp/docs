---
id: Modbus-Settings
title: Modbus Settings
description: Configuration options for enabling and customizing EMS-ESP Modbus/TCP functionality
---

# Modbus-Einstellungen

Aktivieren Sie die Modbus-Funktion in `Settings` -> `Application` -> `Modbus`. Dies erfordert einen Neustart.

## Konfigurierbare Optionen

| `Option name` | `Description` | `Default value` |
| ----------- | -------------------------------------------------------- | ------------- |
| Max Clients | Die maximale Anzahl von Clients, die eine Verbindung herstellen dürfen | 10 |
| Port | Der Port, auf dem der Modbus-Server läuft | 502 |
| Zeitüberschreitung | Die Anzahl der Millisekunden, nach denen Anfragen eine Zeitüberschreitung erfahren | 10000 |
